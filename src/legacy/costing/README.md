# Legacy Costing Module

These files capture the pre-standalone costing implementation that relied on global baseline contexts.
They are retained for historical reference only and should not be imported into new features.
